package index

// Keeping track of key collisions.
type Collisions struct {
	Keys []Key
}

// Load collisions from file. Create file if it doesn't exists.
func LoadCollisions(path string) (*Collisions, error) {
	return nil, nil
}
